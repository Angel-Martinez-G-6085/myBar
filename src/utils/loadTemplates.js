import { loadFooter } from '../templates/footer/footer';
import { loadHeader } from '../templates/header/header';

const loadTemplates = () => {
  loadHeader();
  loadFooter();
};
export default loadTemplates;
